import Section from '../components/sectionComponent'

const background = () => {
    return (
        <Section title="Background">
            <p>Prism is an lightweight yet powerful Javascript library created
            for React To allow developers to produce tween animations on elements
with any CSS and transform value.</p>
        </Section>
    )
}

export default background
