import Section from '../components/sectionComponent'

const react = () => {
    return (
        <Section title={"Implementing prism in React"}>
            <p>Prism also takes advantage of reacts use of JXS to provide an
            PrismComponent and inline Component version of the Prism
Function. </p>

            <p>This component creates an div element to incase all the child elements
            You wish to animate. This Component can also pass all default HTMLElement
Props like onClick, Styles, className.</p>
        </Section>
    )
}

export default reac
